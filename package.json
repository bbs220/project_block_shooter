{
  "name": "project_block_shooter",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -k --names \"SERVER,CLIENT\" -c \"green,blue\" \"npm run dev -w appServer\" \"npm run dev -w appClient\"",
    "build": "npm run build --workspaces --if-present",
    "start": "concurrently -k --names \"SERVER,CLIENT\" -c \"green,blue\" \"npm run start -w appServer\" \"npm run preview -w appClient\""
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "concurrently": "^9.2.4",
    "nodemon": "^3.1.14",
    "tsx": "^4.23.1",
    "typescript": "^6.0.3"
  }
}
